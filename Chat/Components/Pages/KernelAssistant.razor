@page "/assistant/kernel"
@using Chat.Services
@rendermode InteractiveServer

<Assistant Service="@Service" />

@code {
    [Inject(Key = "kernel")]
    public IAssistantService Service { get; set; }
}
